Ієрархія файлів WP на Kama прочитати статтю

оембет для роликів з ютуба
index.php		основа, затичка
header.php (footer.php)	аналогічно  
functions.php		файл батя де є всі стилі і скріпти, настройки.


<?php wp_head(); ?>	команда події виведення header
<?php wp_footer(); ?>	фкція запуску футера
<?php bloginfo('url')?> фкція посилання на головну сторінку  ( echo home_url(); )
	('name')	фкція виводу імені сайта 


Хуки-	підключитись до якоїсь події. WP виводить події поступово,
	завдяки чому можна підйобуватись в якихось місцях.(виведення 
	header, scripts, вн дані сторінки, footer)
   Actions(дія)		-"зроби мою мою дію"
   Filters(фільтр)	-"фільтрує виводимі дані і якось їх обробляє"



	підключення CSS & JS до WP
  add_action('$tag, $function_to_add, $priory = 10, $accepted_args = 1')
	   подія    яку фкцію виконати  пріорітет   аргументи
  add_action('wp_enqueue_scripts', 'style' );
		function style() {
		  wp_enqueue_style('st_main', get_stylesheet_uri()); 		функція підлючення головного файлу style.css
		  wp_enqueue_style('any_werd', get_template_directory_uri() . '/assets/css/style.css' );  } css нашої верстки
  add_action('wp_footer', 'scripts');					піделючення скріптів до події wp_footer в кінці файла.
  		function scripts() {
		  wp_enqueue_script('init', get_template_directory_uri() . '/assets/js/init.js');


 wp_enqueue_scripts - дія коли WP виводить свої скріпти та стилі
 wp_enqueue_style( $handle, $src, $deps, $ver, $media );
   назва того, що ми хочемо виводити; шлях до елемента; від яких файлів залежить; версія; медіазапит
	


	Реєстрація меню сайта

  add_actioun('after_setup_theme', 'theme_register_nav_menu');		реєструємно будь яке меню в function.php
		function theme_register_nav_menu() {
		  register_nav_menu('topNav' 'headerNavBar');}

 wp_nav_menu(array( 'theme_location' => 'top' ітд)); ?>			оголошуємно наше меню в верстці


	Сайтбар